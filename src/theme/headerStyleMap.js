export const headerStyleMap = {
  "/resiliencecenter": { bgcolor: "background.default" },
  "/dashboard": { bgcolor: "background.default" },

  // fallback for everything else
  "*": {
    bgcolor: "#fff",
    borderBottom: "1px solid rgba(0,0,0,0.12)",
  },
};
